import 'package:flutter/material.dart';

class AddressEdite extends StatelessWidget {
  const AddressEdite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Address"),
          centerTitle: true,
        )
    );
  }
}
